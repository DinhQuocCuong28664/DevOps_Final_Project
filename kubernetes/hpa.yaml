apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: devops-final-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: devops-final-deployment
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 60   # Scale thêm pod khi CPU vượt 60%
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 70   # Scale thêm pod khi RAM vượt 70%
